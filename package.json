{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "prestart": "lerna run prestart",
    "core": "lerna exec --scope @project/core-components",
    "editor": "lerna exec --scope @project/editor"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
